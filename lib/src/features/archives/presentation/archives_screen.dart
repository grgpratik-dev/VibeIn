import 'package:flutter/material.dart';

import 'components/archives_body.dart';

class ArchivesScreen extends StatelessWidget {
  const ArchivesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return ArchivesBody();
  }
}
